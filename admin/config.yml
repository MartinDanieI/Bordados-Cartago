# admin/config.yml

# Configuración del backend (cómo se conecta a tu código)
backend:
  name: git-gateway
  branch: master 

# Dónde se guardarán las fotos de los productos
media_folder: "img/uploads"
public_folder: "/img/uploads"

# Define las "colecciones" de contenido. En tu caso, solo una: Productos.
collections:
  - name: "productos"
    label: "Productos"
    folder: "_productos"
    create: true
    slug: "{{slug}}"
    fields: # REEMPLAZA ESTA SECCIÓN 'fields' COMPLETA
      - { label: "Nombre del Producto", name: "title", widget: "string" }
      - { label: "Imagen Principal", name: "image", widget: "image" }
      - { label: "Imagen para el Hover", name: "image_hover", widget: "image" }
      - { label: "Precio (solo el número, ej: 320000)", name: "price", widget: "number" }
      - { label: "Categoría", name: "category", widget: "select", options: ["Hombres", "Mujeres"] }
      - { label: "Tipo de Prenda", name: "type", widget: "select", options: ["Camisa", "Pantalón", "Accesorio", "Blusa", "Gabán", "Otro"] }
      - { label: "Descripción Corta", name: "description", widget: "string" }
      # --- INICIO DE LO NUEVO ---
      - label: "Tallas Disponibles"
        name: "sizes"
        widget: "list" # Esto crea una lista donde puedes añadir varias tallas
        field: { label: "Talla", name: "size", widget: "select", options: ["S", "M", "L", "XL", "XXL"] }
      - label: "Colores Disponibles"
        name: "colors"
        widget: "list" # Y otra lista para los colores
        fields: # Cada color tendrá un nombre y un código de color
          - { label: "Nombre del Color", name: "name", widget: "string", hint: "Ej: Beige, Coral, etc." }
          - { label: "Código Hex del Color", name: "hex", widget: "string", hint: "Ej: #D3C5B8" }
      # --- FIN DE LO NUEVO ---